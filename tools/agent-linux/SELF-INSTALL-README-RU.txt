Установка iSashaVPN / Cudy agent

1. Распакуйте этот архив в отдельную папку.

2. Откройте терминал в этой папке и выполните:

   bash ./install.sh

Если в архиве или рядом есть файл `DC_via_Cudy-linux-self-install.sh`, это тот
же установщик с длинным именем. Для простоты запускайте `install.sh`.

Скрипт сам остановит старый агент, удалит старые файлы из этой папки,
распакует свежий пакет, установит агент в выключенном состоянии и создаст
ярлык `Cudy Agent` на рабочем столе.

После установки агент НЕ запускается автоматически. Для включения/выключения
откройте ярлык `Cudy Agent`.

Если интернет сломался, выполните:

   sudo ./restore_direct.sh
